import { expect } from 'chai';

describe('SandboxBrowserContext', function(){
    //TODO: This design is very hard to test, and if we do it, the tests will be very fragile
    //I'm going to test this functionality with acceptance tests
    xit('should define create method on SandboxRegistry', function(done){
    });

    xit('should send incoming messages to MiniBus', function(){
    });

    xit('should dispatch MiniBus messages to outside', function(){

    });
});
